Linux Mini Terminal

A Linux terminal implemented in C programming language using concepts of fork,
exec, signals, pipes etc. Different kinds of interrupts were also handle


1. Run make
2. Run Executable ./shell

For exit, type "exit"

